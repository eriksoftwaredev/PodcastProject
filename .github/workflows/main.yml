name: Generate Podcast Feeds
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4.1.2
      - name: Run Feed Generator
        uses: eriksoftwaredev/Podcast-Generator-GH-Action@Master
